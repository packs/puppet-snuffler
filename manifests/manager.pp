class snuffler::manager {

  package {
    snuffler-manager:
      ensure => installed;
  }

}

# vim modeline - have 'set modeline' and 'syntax on' in your ~/.vimrc.
# vi:syntax=puppet:filetype=puppet:ts=4:et:
